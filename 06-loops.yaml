- name: Installing packages
  hosts: mongodb
  become: yes
  tasks:
    - name: Install packg
      ansible.buildin.yum:
        name: "{{item}}"
        state: installed
      #list of pckges
      loop:
      - nginx
      - postfix
